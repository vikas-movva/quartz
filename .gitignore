# Quartz build outputs (regenerated on every CI build)
public/
content/

# dependencies
node_modules/

# local plugin cache (the registry cache, NOT .quartz/plugins which holds our code)
quartz/.quartz-cache/
.quartz/cache/

# misc
.DS_Store
*.log
